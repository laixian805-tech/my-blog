# 更新记录

这个文件放在 `README.md` 同级目录，用来记录站点每次修改后的内容，方便回看“改了什么、改了哪些文件、有没有验证通过”。

## 使用规则

- 每次改动后，按日期新增一条记录，不覆盖旧内容。
- 每条记录尽量写清楚 4 件事：修改主题、涉及文件、具体变化、验证结果。
- 如果只是新增文章或笔记，也建议记录来源目录、导入文件名和是否公开。
- 如果改动还没有提交到 GitHub，请明确写成“当前工作区，未提交”。

---

## 2026-03-19（已提交，commits: `e602dba` ~ `b02eb44`）

### 主题：GitHub Pages 路由与搜索修复、导航门户稳定性和首页状态区刷新

### 涉及文件

- `data/blog/github-pages-troubleshooting-checklist.mdx`
- `data/projectsData.ts`
- `components/SiteStatusPanel.tsx`
- `components/nav/NavPortal.tsx`
- `components/HomeProfileHero.tsx`
- `lib/getLastUpdated.ts`
- `app/blog/[...slug]/page.tsx`
- `app/blog/page.tsx`
- `app/blog/page/[page]/page.tsx`
- `components/MobileNav.tsx`
- `components/Image.tsx`
- `lib/sitePath.ts`
- `app/search/page.tsx`
- `components/SearchPageClient.tsx`
- `components/SiteSearchForm.tsx`
- `components/StaticSiteSearchForm.tsx`
- `public/static/nav-icons/lintcode.ico`
- `public/static/nav-icons/shixiseng.ico`

### 修改内容

- 新增 GitHub Pages 排障清单文章，并补到项目入口数据中。
- 刷新首页状态区和导航门户样式，引入最近更新时间等展示信息，调整门户卡片和首页状态模块的视觉结构。
- 恢复并补强部分彩色导航站点图标，顺带修正博客详情、分页和移动导航在静态部署下的路由稳定性。
- 抽出 `lib/sitePath.ts` 统一处理静态导出时的 `basePath`，修复图片、导航门户和搜索表单在 GitHub Pages 下的路径拼接问题。
- 重做搜索页客户端逻辑和交互，增强静态搜索体验，并继续修复搜索导航里重复拼接 `basePath` 的问题。

### 验证结果

- Git 历史确认 2026-03-19 共 8 次提交，提交区间为 `e602dba` 到 `b02eb44`。
- 这一天的变更合计涉及 23 个文件，约 `+1271 / -173`。
- 当时未同步补写本文件，仓库里也没有保留单独的构建或回归验证记录；本条为按提交历史回填。

### 备注

- `更新记录.md` 未在 2026-03-19 当天被修改，所以这条记录此前缺失。

---

## 2026-03-17（已提交）

### 主题：Blog Stats 仪表盘视觉轻量化优化

### 涉及文件

- `components/HomeProfileHero.tsx`
- `components/HomeSiteStats.tsx`
- `更新记录.md`

### 修改内容

- 删除 `Blog Stats` 标题下方的长说明文字，保留更干净的顶部结构。
- 将 `Overview` 从按钮感样式改成弱化的浅蓝状态标签，减少视觉抢占。
- 重做四张统计卡的排版、边框和阴影，让数字更突出、层级更清晰。
- 将统计数字显式切换为系统无衬线粗体风格，弱化全站展示字体带来的装饰感。
- 精简“内容分布”和“站点状态”区域，去掉站点状态里的灰色胶囊框，并统一卡片质感。

### 验证结果

- `yarn build`：通过

### 备注

- 这一轮只优化首页统计面板视觉，不改统计口径和首页 Hero 的整体结构。

---

## 2026-03-17（已提交）

### 主题：乱码清理、首页文案校准与搜索索引瘦身

### 涉及文件

- `data/siteMetadata.js`
- `app/Main.tsx`
- `app/blog/page.tsx`
- `components/HomeProfileHero.tsx`
- `components/HomeSiteStats.tsx`
- `components/SiteSearchForm.tsx`
- `components/SearchPageClient.tsx`
- `app/search/page.tsx`
- `app/security/page.tsx`
- `更新记录.md`

### 修改内容

- 把首页、博客列表、搜索页、Security 列表、站点元数据中的残留乱码文案统一改回正常中文。
- 修正首页 Hero 中仍在引用 `Vercel Analytics` 的说明文字，使它和当前实现一致。
- 调整首页统计区文案，改成“站内概览 + 内容分布 + 建站信息”的真实状态，不再误导成实时访客统计。
- 将搜索索引从“整篇正文直传客户端”改成“标题 + 摘要 + 截断后的正文片段”，减少 `/search` 页载荷。
- 在搜索结果页使用 `useDeferredValue`，让客户端筛选在内容增多后更平滑。
- 顺手补齐了搜索框、博客列表和 Security 列表里的中文可访问性文案。

### 验证结果

- `yarn build`：通过

### 备注

- 这轮主要解决的是“显示错误”和“前端负载过重”，不涉及部署平台切换。
- 中国大陆手机网络访问 `vercel.app` 的稳定性问题，仍然需要靠自定义域名或更合适的托管方案解决。

---

## 2026-03-17（已提交，commit: `7890f5e`）

### 主题：移动端稳定性整理与修改记录文档落地

### 涉及文件

- `app/layout.tsx`
- `next.config.js`
- `app/search/page.tsx`
- `components/SearchPageClient.tsx`
- `components/Footer.tsx`
- `data/security/vulnhub-notes.mdx`
- `data/siteMetadata.js`
- `package.json`
- `yarn.lock`
- `更新记录.md`

### 修改内容

- 移除了首页运行时注入的统计脚本依赖，减少页面额外请求。
- 收紧了 `CSP` 白名单，去掉当前未使用的外部脚本和 iframe 域名。
- 把 `/search` 改造成可兼容静态导出的结构。
- 重写页脚社交图标渲染方式，修复类型问题。
- 移除了安全笔记中的一张外部图床图片。
- 增加本文件，作为后续统一的修改记录入口。

### 验证结果

- `yarn build`：通过
- `EXPORT=1 yarn build`：通过

---

## 2026-03-17（已提交，commit: `dc93bb9`）

### 主题：首页统计方案调整

### 修改内容

- 移除了原先不稳定的 `Busuanzi` 首页统计方案。
- 接入过 `Vercel Analytics` 方向的统计方案。
- 把首页统计卡改成站内内容数量与建站时间组合。
- 将 `siteUrl` 改成真实的 Vercel 地址。
- 去掉旧的 GitHub Pages 工作流，统一收口到 Vercel 部署。

---

## 2026-03-17（已提交，commit: `9efe7be`）

### 主题：模板改造成个人博客首版

### 修改内容概览

- 模板从默认英文技术博客改成个人中文知识博客。
- 顶部导航改成 `Home / Blog / Nav / Projects / Security`。
- `Tags` 被替换为 `Nav` 导航门户页。
- `About` 被替换为 `Security` 网安区域。
- 接入普通博客文章与网安笔记两套内容。

---

## 2026-03-17（已提交，commit: `0da4f9f`）

### 主题：项目初始化

- 初始博客仓库落地。

---

## 2026-03-20（当前工作区，未提交）

### 主题：中文站点连通性排查、站点地图链接规范化与搜索/状态展示细节调整

### 涉及文件

- `app/robots.ts`
- `app/search/page.tsx`
- `app/sitemap.ts`
- `components/Link.tsx`
- `components/SearchPageClient.tsx`
- `components/SiteStatusPanel.tsx`
- `data/portalData.ts`
- `layouts/AuthorLayout.tsx`
- `.playwright-cli/page-2026-03-19T13-15-30-369Z.yml`
- `output/playwright/cn-sites/leetcode-cn-after.png`
- `output/playwright/cn-sites/leetcode-cn.har`
- `output/playwright/cn-sites/nowcoder-after.png`
- `output/playwright/cn-sites/nowcoder.har`
- `output/playwright/cn-sites/programmercarl-after.png`
- `output/playwright/cn-sites/programmercarl.har`
- `public/static/nav-icons/lintcode.ico`
- `public/static/nav-icons/shixiseng.ico`

### 修改内容

- 从 `robots.ts` 去掉 `host` 字段，并在 `sitemap.ts` 中统一把站内 URL 规范成带尾部 `/` 的绝对地址，减少静态部署下的链接歧义。
- 从搜索结果页移除页内的“搜索”大标题、说明文字和第二个搜索框，只保留顶部全站搜索入口与原有结果列表逻辑；同时简化 fallback，避免加载态再出现一整块冗余说明。
- 调整 `components/Link.tsx`，兼容非字符串 `href`，避免对象型链接属性时报错。
- 在 `components/SearchPageClient.tsx` 中让查询词变化时自动切回 `all` 标签，避免旧标签状态残留。
- 在 `components/SiteStatusPanel.tsx` 中先用回退值渲染 UV/PV，再异步尝试实时数据，减少“同步中”占位。
- 从 `data/portalData.ts` 移除 `实习僧` 和 `LintCode` 的本地图标引用，并尝试把 `programmercarl.com` 切到 `http://` 以配合当前连通性排查。
- 保留 Playwright 截图和 HAR 作为排查产物；`layouts/AuthorLayout.tsx` 只有类名顺序微调，无行为变化。

### 验证结果

- 已生成 3 组 Playwright 截图与 HAR：`leetcode.cn`、`nowcoder.com`、`programmercarl.com`。
- 当前 HAR 中 3 个请求都记录为 `net::ERR_CONNECTION_CLOSED`。
- `yarn build`：通过。

### 备注

- 当前为暂存区改动，尚未提交到 GitHub。
- 包含测试产物和图标删除；如果后续不需要保留，可在提交前再清理。

---

## 后续新增记录模板

复制下面这段，直接追加到文件末尾即可：

```md
## YYYY-MM-DD（当前工作区，未提交 / 已提交，commit: `xxxxxxx`）

### 主题：本次修改主题

### 涉及文件

- `路径/文件1`
- `路径/文件2`

### 修改内容

- 改了什么
- 为什么改
- 对页面或部署有什么影响

### 验证结果

- `yarn build`：通过 / 未执行
- 其他验证：例如本地访问、移动端测试、导出测试

### 备注

- 还没解决的问题
- 需要下次继续跟进的点
```
